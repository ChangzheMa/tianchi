# 06 · 关键风险与行动清单（必读）

> 本文档是整理过程中发现的**文档间矛盾、代码过时问题、易踩的坑**，以及优先级建议。
> 这些不在原始文档里，但直接决定能否拿到分。

## 🔴 一级风险：`main.py` 是过时旧版，上不了 A/B 榜

根目录 `main.py` 与 `baseline教程.txt` 描述的**不是同一套方案**。它是"旧样例数据"版本，
若直接拿去跑真实 A/B 榜数据会失败：

| 问题 | main.py（旧） | 真实赛题要求 / baseline 教程 |
|------|--------------|------------------------------|
| **资金分类** | `{游资, 量化机构}` **二分类** | `{游资, 量化, 散户}` **三分类** |
| **类别命名** | 用 "量化机构" | 必须是 "量化"（否则报错） |
| **数据源** | 单个 Excel，英文列 `symbol/date/bids/asks` | 日期/股票/三个 GBK 中文列名 CSV |
| **时间字段** | UTC 毫秒 `date` + `hh` 北京时 | `HHMMSSmmm` 整数 |
| **盘口** | `bids/asks` JSON 字符串 | 十档 `申买/卖价1~10` + `量1~10` 列 |
| **OSS 分级** | 按**成交量**阈值（≥50000） | 按**成交额**阈值（≥50万元） |

后果：
- `main.py` 的 `save_results` 里 `assert capital_type.isin(['游资','量化机构'])`，
  而 `capital_intention` 断言也不含"散户"逻辑——**输出的标签体系本身就是非法的**（缺散户）。
- `load_and_preprocess` 假设 `df['bids']`、`df['hh']`、`df['dt']` 等列，真实三源 CSV **没有这些列**，读取即崩。

👉 **行动**：以 `baseline教程.txt`（文档 04）为准**重写数据层 + 改三分类**，
`main.py` 仅可作旧样例参考，别在它上面改补丁。

## 🟠 二级风险：文档间的数值/日期矛盾（以官方页面为准）

| 项 | 说法 A | 说法 B | 建议 |
|----|--------|--------|------|
| A 榜起始日 | 06-09（baseline 教程） | 06-08（main.py 头注释） | 以天池官方页面为准 |
| Task2 真值回溯 | T+5 日（baseline 正文） | T+8 日（main.py 头注释） | 都是"未来回溯定真值"，非日常分数；不影响做法 |
| 提交编码 | UTF-8 + LF（CLAUDE.md/官方） | utf-8-sig 带 BOM（代码/教程） | **用不带 BOM 的 UTF-8 + LF**，避免 BOM 干扰 |
| 样例股 | 继峰 603997.SH（baseline 教程） | CLAUDE.md 举例用 603316 | 603316 只是格式示例，样例数据是 603997 |

## 🟡 三级风险：提交格式细节

1. **行数**：正式提交每个 CSV **100 行**（100 只股票 × 前一交易日单日）。
   提交样例里是多日多行（随机演示），**别照抄行数**。
   当前 `submit/` 只有 1 行占位数据，且 `pattern_reco.csv` 里有一个 UTF-8 乱码字符（"日内价�?"），
   正式提交前必须替换。
2. **股票代码后缀**：提交去掉 `.SH/.SZ`。注意提交样例用的是 **`.SZ`（深市）** 代码，
   而 CLAUDE.md 写"100 只沪市股票 / 去 `.SH`"——**以 `股票样本.xlsx` 实际内容为准**（尚未核对，见待办）。
3. **`transaction_date` 必须是前一交易日**，写错日期直接报错/0 分。
4. **停牌股**可删该行，但两个 CSV 的股票/行要保持一致。

## 🎯 评分策略（决定资源投向）

A 榜真正计分的只有两处，务必对齐权重：

```
总分 = pattern_type 聚类质量 × 0.4  +  capital_type 加权F1 × 0.6
```

- `capital_intention`（买入/卖出/T0）→ **不上 A 榜分**，仅 B 榜可解释性。
- `pattern_explanation`（模式文案）→ **不上 A 榜分**，仅 B 榜可解释性。

👉 **A 榜阶段**：把主力压在**三分类 F1（60%）**和**聚类区分度/聚合度（40%）**上；
意图与文案够用即可，别过度打磨。
👉 **B 榜阶段**：再补可解释性（explanation 文案质量、intention 合理性、方案报告）。

## ✅ 行动清单（建议顺序）

- [ ] **核对 `股票样本.xlsx`**：确认 100 只股票是沪市还是含深市、代码格式，定"去后缀"规则。
- [ ] **搞定数据获取**：官方不给明细，需自行获取 100 只股票的三源 L2 数据（渠道见 Q&A）。
- [ ] **重写数据层**：按三源 GBK CSV + `HHMMSSmmm` 重写读取（参考 baseline 教程，非 main.py）。
- [ ] **落地三分类**：`capital_type` 改为 `{游资, 量化, 散户}`，修正所有断言与校准逻辑。
- [ ] **跑通样例集**（603997.SH，2026-05-07）验证全流程。
- [ ] **特征优先**：先补 OFI、撤单行为（逐笔委托）两类高 ROI 特征。
- [ ] **对齐提交格式**：100 行、去后缀、UTF-8 无 BOM + LF、`transaction_date` = 前一交易日。
- [ ] **建立每日提交—反馈闭环**：用 A 榜 F1 反推调参（每天 3 次，取最后一次）。
- [ ] **合规自查**：无未来函数、无硬编码个股标签、相对路径、固定随机种子。

## 📌 未纳入文档的原始文件说明

- `算法大赛-天池大赛-阿里云的赛制.mhtml`：天池平台赛制页快照（quoted-printable + base64，
  约 5MB），内容为**平台通用赛制**（与赛题详情页高度重叠），赛题专有信息已由三份 txt 覆盖，
  故未逐字提取。需要平台通用条款（奖金、知识产权、违规处理等）时再单独解析。
- `AFAC2026赛题一训练数据.xlsx`：**旧版样例数据**（对应 main.py 的 Excel 结构），
  与真实三源 CSV 不同，仅供旧 main.py 调试。
